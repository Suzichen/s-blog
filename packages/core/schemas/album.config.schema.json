{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://unpkg.com/@s-blog/core/schemas/album.config.schema.json",
  "title": "S-Blog Album Configuration",
  "description": "Configuration schema for s-blog album/photo gallery settings",
  "type": "object",
  "required": ["enabled", "albums"],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON Schema reference URL for editor intellisense"
    },
    "enabled": {
      "type": "boolean",
      "description": "Enable or disable the album module"
    },
    "albums": {
      "type": "array",
      "description": "List of album configurations",
      "items": {
        "type": "object",
        "required": ["dir"],
        "properties": {
          "dir": {
            "type": "string",
            "description": "Directory name relative to public/albums/"
          },
          "name": {
            "type": "string",
            "description": "Display name for the album (optional, defaults to dir)"
          },
          "cover": {
            "type": "string",
            "description": "Cover image filename (optional, auto-selected if not set)"
          }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
