[package]
name = "create-s-blog"
version = "0.1.0"
edition = "2021"
description = "NAPI-RS bindings for s-blog-scaffold — exposes scaffold to Node.js"
license = "MIT"

[lib]
name = "create_s_blog"
crate-type = ["cdylib"]
path = "rust-src/lib.rs"

[dependencies]
s-blog-scaffold = { path = "../../crates/s-blog-scaffold" }
napi = { version = "2", default-features = false, features = ["napi4"] }
napi-derive = "2"

[build-dependencies]
napi-build = "2"
