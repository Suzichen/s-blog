import type { AlbumConfig } from '@s-page/core';

export const albumConfig: AlbumConfig = {
  enabled: true,
  albums: [
    { dir: 'blog' },
    { dir: 'Sakura', name: '春', cover: 'DSC_1666.jpg' },
  ],
};
