> *Note: This is the original s-blog project, which has now been migrated and renamed to Spage.*

# `@spage/engine-linux-x64-gnu`

Platform-specific native binary for [`@spage/engine`](https://www.npmjs.com/package/@spage/engine) on Linux x64 (glibc).

This package is installed automatically as an optional dependency — you should not need to install it directly.

See the [main package](https://www.npmjs.com/package/@spage/engine) for API documentation.
